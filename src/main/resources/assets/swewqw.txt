entryNotFound

Commands

    /wiis reload

    /wiis <enabled|autosave>

    /wiis modify <mod:entry> reason <reasonKey> <bool>
                             nbt add <nbtComparable>
                                 remove <nbtPath>
                                 clear
                             despawnInstantly <bool>
                             invert

    /wiis query <mod:entry> | reason [reasonKey]
                              nbt [nbtPath]
                              despawnInstantly
    /wiis restore [mod:entry]


nbt filter syntax:

    Number (int, float, double, short, long etc.)
    String (just string)
    Boolean
    Object
    Array

    path{to}{'nbt'}{"val"}:comparable_operator;
    path{to}{nbt}{array}[index]:comparable_operator;

    path{to}{force lead}:>=3; num
    display{name}:=="spin"; string
    test{is}:==true; (bool()) <-> test{is}; (has() + bool()) boolean
    hello; (has()) value
    hello[index]; (index.has()) array

    ?minecraft:.*
    age>2;
    name:@="pattern"
    hello{world};




{
    excludedReasons: [SpawnReason], -> []
    excludedNbt: [str],
    despawnInstantly: boolean
}


Entry(list[str], list[str], bool)

    checkReason(str)
    setReason(str, bool)

    checkNbt(str): bool
    addNbt(str)
    removeNbt(str)
    clearNbt()

    despawnInstantly(): bool
    despawnInstantly(bool)

    invert()


Config()

    editEntry(str, (entry) ->)
    allowSpawn(str, reason, list[nbt])
    allowSpawn(str, reason)
